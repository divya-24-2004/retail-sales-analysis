# Retail Sales Performance Analysis

## Project Overview
This project analyzes retail data to identify seasonal trends and top-performing product categories.

## How to Run
1. Clone the repository.
2. Install dependencies: `pip install pandas matplotlib`.
3. Open `analysis.ipynb` in Jupyter Notebook.
